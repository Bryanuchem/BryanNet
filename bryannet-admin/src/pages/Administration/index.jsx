import Typography from "@mui/material/Typography";

function Devices() {
    return (
        <Typography variant="h4">
            Administration
        </Typography>
    );
}

export default Devices;